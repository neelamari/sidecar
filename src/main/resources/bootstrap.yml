spring:
  cloud:
    config:
      enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://cdts-eureka.mybluemix.net/eureka/
  instance:
    lease-renewal-interval-in-seconds: ${eureka-interval:5}
    prefer-ip-address: false

security:
  basic:
    enabled: false    